'use client';

import BaseStandings from '@/components/common/standings/BaseStandings';

export default function FutsalStandingsPage() {
  return <BaseStandings title="Standings — Futsal" hasGroups={false} standings={[]} games={[]} />;
}
