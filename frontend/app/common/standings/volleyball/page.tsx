'use client';

import BaseStandings from '@/components/common/standings/BaseStandings';

export default function VolleyballStandingsPage() {
  return <BaseStandings title="Standings — Volleyball" hasGroups={false} standings={[]} games={[]} />;
}
