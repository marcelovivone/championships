'use client';

import BaseStandings from '@/components/common/standings/BaseStandings';

export default function IceHockeyStandingsPage() {
  return <BaseStandings title="Standings — Ice Hockey" hasGroups={false} standings={[]} games={[]} />;
}
