'use client';

import BaseStandings from '@/components/common/standings/BaseStandings';

export default function BasketballStandingsPage() {
  return <BaseStandings title="Standings — Basketball" hasGroups={false} standings={[]} games={[]} />;
}
