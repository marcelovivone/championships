'use client';

import BaseStandings from '@/components/common/standings/BaseStandings';

export default function HandballStandingsPage() {
  return <BaseStandings title="Standings — Handball" hasGroups={false} standings={[]} games={[]} />;
}
